# components/ui

This folder is reserved for `shadcn/ui` primitives.

Rule:

- Use `shadcn` CLI components here first.
- Add custom components in `components/cms` only when composition is not enough.
