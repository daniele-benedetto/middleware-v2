import { publicContentClassName } from "@/components/public/primitives";

export const publicHeaderBarClassName = `${publicContentClassName} flex items-center justify-between gap-6 py-3.75`;

export const publicHeaderButtonClassName =
  "flex cursor-pointer items-center gap-3.25 border-0 bg-transparent font-heading transition-colors duration-(--motion-fast) md:hover:text-accent";
